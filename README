Psudo-code for a boat navigation system. Its in C++ and doesnt compile cause its psudocode. It would compile... probably... if there was actually a library that implemented all the correct functions from the fake boat control library. Even then, it probably wouldnt...
